version: "3.9"

services:
  dev:
    build: .
    container_name: dev-container
    volumes:
      - .:/app
    ports:
      - "3000:3000"  
    command: npm run dev  
    stdin_open: true
    tty: true
